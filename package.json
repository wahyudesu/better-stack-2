{
  "name": "better-stack-2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:web": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server dev",
    "deploy": "pnpm --filter @better-stack-2/infra deploy",
    "destroy": "pnpm --filter @better-stack-2/infra destroy"
  },
  "dependencies": {
    "@better-stack-2/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@better-stack-2/config": "workspace:*",
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "^22.13.14",
    "typescript": "^5"
  },
  "packageManager": "pnpm@10.27.0"
}
