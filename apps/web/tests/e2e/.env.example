# Test user credentials (must exist in Clerk)
TEST_CLERK_EMAIL=test@example.com
TEST_CLERK_PASSWORD=yourpassword123

# Zernio API key (optional — skip zernio tests if not set)
TEST_API_KEY=

# URLs (optional — defaults to localhost:3000)
E2E_BASE_URL=http://localhost:3000
E2E_SERVER_URL=http://localhost:8787